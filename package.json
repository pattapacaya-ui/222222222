{
  "name": "legionauth",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:api": "pnpm --filter @legionauth/api dev",
    "dev:web": "pnpm --filter @legionauth/web dev",
    "build": "pnpm --filter @legionauth/web build",
    "deploy": "bash deploy.sh"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
